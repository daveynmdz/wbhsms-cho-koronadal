# Web-Based Healthcare Services Management System
# Environment Configuration Template for XAMPP

# Database Configuration (XAMPP Default Settings)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=wbhsms_database
DB_USER=root
DB_PASS=

# Email Configuration (Required for appointment confirmations)
# For Gmail: Use App Password (not your regular password)
# Enable 2-factor authentication and generate an app password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# System Email Settings
FROM_EMAIL=noreply@chokoronadal.gov.ph
FROM_NAME=CHO Koronadal Health System

# System Information
SYSTEM_URL=http://localhost/wbhsms-cho-koronadal
CONTACT_PHONE=(083) 228-8042
CONTACT_EMAIL=info@chokoronadal.gov.ph
FACILITY_ADDRESS=Koronadal City, South Cotabato

# Application Settings
APP_DEBUG=1
APP_ENV=development

# Debug Settings (set to true for email debugging)
DEBUG_EMAIL=false

# Instructions:
# 1. Copy this file to .env
# 2. Update database credentials (XAMPP default user is 'root' with no password)
# 3. Create database 'wbhsms_database' in phpMyAdmin
# 4. Import database/wbhsms_database.sql
# 5. Configure SMTP settings for email features:
#    - For Gmail: Enable 2-factor authentication
#    - Go to "App passwords" and generate a new app password
#    - Use that app password in SMTP_PASSWORD (not your regular password)
#    - Update SMTP_USERNAME with your Gmail address